# Bubble Breath — 기획문서

## 0. 프로젝트 개요

- 프로젝트명: Bubble Breath
- 프로젝트 폴더: `bubble-breath`
- 플랫폼: 모바일 웹 우선
- 형태: 마이크를 사용하는 인터랙티브 디지털 장난감

**한 줄 설명**
휴대폰에 실제로 `후—` 불면 비눗방울이 만들어지고, 불기를 멈추면 방울이 날아가는 작은 웹 장난감.

이 프로젝트의 목적은 게임을 만드는 것이 아니다.
핵심은 단 하나다.

> 내 실제 행동이 화면 속 물체와 직접 연결되어 있다는 느낌.

사용자가 처음 실행한 뒤 5초 안에 `후— → 방울이 커짐`을 경험해야 한다.

## 1. 핵심 경험

사용자는 화면에 들어온다. 화면 중앙 하단에는 비눗방울 막대가 있다.

```
       후— 불어봐



          ○
          │
          │
          │
```

마이크 사용을 허용하고 실제 휴대폰 마이크에 바람을 분다. 그러면 막 끝에 비눗방울이 생긴다.

```
        ◯
        │
        │
```

계속 불면:

```
        ⭘
        │
```

더 불면:

```
       ◯◯
      ◯  ◯
       ◯◯
        │
```

방울이 점점 커진다. 그리고 사용자가 불기를 멈추면:

```
        ◯
         ↗

        │
        │
```

막에서 `똑` 떨어져 위로 날아간다.

화면에 떠다니는 방울을 터치하면 팡! 하고 터진다. 곧바로 다음 방울을 다시 만들 수 있다.

## 2. 핵심 플레이 루프

전체 경험은 무한 반복된다.

> 💨 불기 → 🫧 커지기 → ✨ 떨어지기 → 🫧 둥실거리기 → 👆 터뜨리기 → 💨 또 불기

별도의 게임 시작/종료가 없다. 점수도 없다. 게임 오버도 없다. 사용자가 질릴 때까지 그냥 가지고 논다.

## 3. 가장 중요한 제품 원칙

### ① 반응이 즉각적이어야 한다

사용자가 불기 시작했는데 0.5~1초 뒤에 방울이 움직이면 재미가 크게 떨어진다. 가능한 한 `후— → 즉시 방울 반응`이 느껴져야 한다.

### ② 방울 자체가 피드백 UI다

다음과 같은 UI를 만들지 않는다.

```
WIND POWER

███████□□□
72%
```

마이크가 얼마나 입력되고 있는지는 방울을 보면 알 수 있어야 한다. 약하게 불면 천천히 커지고, 강하게 불면 빠르게 커진다.

### ③ 완벽한 원이면 안 된다

방울이 단순히 CSS `scale()`로 커지는 것처럼 보여서는 안 된다. 비눗방울은 살아 있는 것처럼:

- 살짝 찌그러지고
- 흔들리고
- 늘어났다가
- 다시 둥글어지고
- 막에 붙어 있을 때 조금 출렁여야 한다.

이 wobble이 중요한 손맛이다.

### ④ 설명하지 않는다

긴 튜토리얼을 만들지 않는다. 첫 화면의 instruction은 최대 `후— 불어봐` 정도다. 터치 가능한 방울이 처음 만들어졌을 때 필요하다면 최초 한 번 `톡!` 정도를 보여줄 수 있다.

## 4. 첫 진입

화면을 열자마자 장난감이 보여야 한다. Splash screen이나 랜딩페이지를 만들지 않는다.

첫 화면

```
┌─────────────────────┐
│                     │
│                     │
│                     │
│                     │
│     후— 불어봐      │
│                     │
│          ○          │
│          │          │
│          │          │
│                     │
│    [ 불어보기 ]     │
│                     │
└─────────────────────┘
```

`불어보기` 버튼을 누르면 마이크 권한을 요청한다. 권한을 허용하면 버튼은 사라진다. 이후 전체 화면이 장난감 영역이다.

## 5. 왜 버튼을 한 번 누르게 하는가

브라우저의 마이크 접근 및 오디오 관련 제약 때문에 첫 사용자 gesture가 필요할 수 있다. 따라서 사이트 접속 즉시 권한 팝업을 띄우기보다 `[불어보기]`를 사용한다.

사용자가 누르면:

1. 마이크 권한 요청
2. AudioContext 활성화
3. 주변 소음 측정
4. 플레이 시작

을 처리한다.

## 6. 마이크 / 바람 감지

`getUserMedia()` + Web Audio API를 사용한다. 목표는 사람 목소리를 정확하게 분석하는 것이 아니다. 마이크 근처에 바람을 부는 행동을 감지하는 것이 목적이다.

기본 구조

```
microphone
↓
Web Audio API
↓
audio level / frequency characteristics
↓
smoothing
↓
breathStrength
↓
bubble growth
```

내부적으로 `breathStrength = 0 ~ 1` 정도의 normalized 값을 만들어 사용한다.

## 7. 주변 소음 처리

마이크를 켠 직후 짧은 시간 동안 ambient noise를 측정한다. 이를 baseline으로 사용한다.

```
input = currentMicLevel - ambientNoise
```

다만 raw amplitude 하나만으로 바람을 판단하면 말소리 / 음악 / 주변 사람 / 차량 소리에도 반응할 수 있다. 초기 버전에서는 완벽한 breath classifier까지 만들 필요는 없지만, 가능하면 바람에서 강하게 나타나는 broadband/noisy signal 특성을 함께 활용한다.

**중요한 것**: 완벽한 판별보다 "내가 불었을 때 확실히 반응한다"가 우선이다.

## 8. 마이크 Calibration

기기마다 마이크 감도가 다르다. 따라서 코드에 하나의 절대 threshold를 박아두고 끝내지 않는다. 초기 ambient 측정값을 기반으로 threshold를 동적으로 결정한다.

또한 개발 중 쉽게 조절할 수 있도록 다음 값은 상수/config로 분리한다.

```
BREATH_THRESHOLD
BREATH_MAX
SMOOTHING
GROWTH_RATE
RELEASE_DELAY
```

## 9. 방울 생성 상태

한 개의 방울은 대략 다음 state를 가진다.

```
idle → forming → attached → released → floating → popped
```

- **idle**: 막만 존재.
- **forming**: 바람이 감지되면서 아주 작은 방울 생성.
- **attached**: 막 끝에 붙어 있는 상태로 계속 성장.
- **released**: 불기가 멈춰 막에서 떨어지는 순간.
- **floating**: 화면 안을 자유롭게 이동.
- **popped**: 터지는 애니메이션 후 제거.

## 10. 불기 → 방울 생성

바람이 threshold를 넘으면 새로운 방울을 만든다. 처음부터 큰 방울이 갑자기 나타나면 안 된다. 막의 링 부분에서 작은 막이 `ぷく…` 하고 시작되는 느낌이어야 한다. 처음 크기는 매우 작다. 계속 불수록 방울의 target radius가 증가한다.

```
radius += breathStrength * growthRate * deltaTime
```

단, 실제 렌더링 값에는 easing / smoothing을 적용한다.

## 11. 불기 강도

- 약하게: 천천히 부풀기
- 보통: 기분 좋게 부풀기
- 강하게: 빠르게 부풀면서 wobble 증가

즉 강한 바람은 단순히 크기만 빨리 키우는 게 아니라 방울의 움직임에도 영향을 준다.

## 12. Wobble

이 프로젝트의 중요한 시각적 요소다. 방울은 완벽한 원이 아니다. 기본적으로 매우 약한 `scaleX`, `scaleY`, `positionX`, `rotation` 변화를 지속적으로 준다.

예:

```
scaleX = 1 + sin(time * speed) * wobble
scaleY = 1 - sin(time * speed) * wobble
```

단순한 sine 하나만 쓰면 기계적으로 보일 수 있으므로 여러 frequency 또는 noise를 섞어도 좋다. 강하게 불수록 wobble amplitude가 조금 커진다.

불기를 멈추면 `뿌우웅 → 뽀용` 하면서 잠깐 출렁인 뒤 떨어지는 느낌을 준다.

## 13. 방울 떨어뜨리기

바람이 멈춘 즉시 떨어뜨리지 않는다. 아주 짧은 grace period를 둔다. 이유는 실제 마이크 입력이 계속 흔들리기 때문이다.

```
breath lost
↓
150~300ms 정도 대기
↓
여전히 breath 없음
↓
release
```

정확한 값은 실제 폰에서 테스트하면서 조정한다.

**Release 연출**: 방울 아래쪽이 잠깐 늘어난다.

```
    ◯
     \
      ○
      │
```

그리고 `똑` 떨어진다. 이 작은 순간이 중요하다.

## 14. 너무 큰 방울

큰 방울을 만드는 것 자체가 재미 요소가 될 수 있다. 따라서 어느 정도까지는 꽤 크게 만들 수 있게 한다. 다만 지나치게 커지면 wobble이 강해진다. 그리고 최대 크기 부근에서는 낮은 확률 또는 특정 조건으로 막에 붙은 상태에서 팡! 하고 터질 수 있다.

**주의**: 이걸 실패/벌칙처럼 만들지 않는다. "ㅋㅋㅋ 너무 크게 불었더니 터졌다."라는 장난스러운 사건이어야 한다. 초기 v0.1에서는 자동 파열을 빼도 된다.

## 15. 방울이 날아가는 방식

released 상태가 되면 방울은 위쪽으로 천천히 이동한다. 기본 힘은 `buoyancy → upward`. 여기에 아주 약한 `horizontal drift`, `noise`, `wobble`을 추가한다.

결과적으로 직선으로 올라가지 않고 둥실거리며 움직인다.

## 16. 여러 개의 방울

새 방울을 만들었다고 기존 방울을 지우지 않는다. 따라서 계속 불면 화면에 방울이 쌓인다. 이것 자체가 작은 보상이 된다. 사용자가 만든 방울들이 화면을 채운다.

성능을 위해 최대 bubble count를 설정한다. 예를 들어 20~40개 정도에서 테스트 후 결정한다. 오래된 방울은 화면 밖으로 떠나가 자연스럽게 제거한다.

## 17. 터치해서 터뜨리기

floating bubble은 터치 가능하다. 방울을 누르면 즉시 `톡!` 하고 터진다.

**애니메이션**: 단순히 `display:none` 하지 않는다. 짧은 순간:

1. 방울이 살짝 눌림
2. 막이 확 퍼짐
3. 작은 물방울/반짝임 몇 개
4. 사라짐

전체 애니메이션은 매우 짧고 즉각적이어야 한다.

## 18. 소리

사운드는 작은 효과만 사용한다.

```
방울 생성    아주 작은 비눗막 소리
release      뽁 / 똑
pop          톡!
large pop    팡!
```

배경음악은 초기 버전에서 넣지 않는다. 특히 마이크를 사용하는 프로젝트이므로 BGM이 breath detection을 방해하지 않도록 한다. v0.1은 효과음조차 없어도 된다.

## 19. 비주얼 방향

유리 프로젝트처럼 고급스럽게 만들지 않는다. 하지만 어린이 교육 앱처럼 만들지도 않는다. 목표는 "깨끗하고 예쁜 디지털 장난감"이다.

**배경**: 아주 밝은 단색 또는 미세한 gradient. 예: sky / cream / very light blue / pale lavender. 복잡한 배경을 만들지 않는다.

## 20. 비눗방울 표현

방울은 단순한 파란 원으로 만들지 않는다. 최소한 다음 요소가 있어야 한다.

- **투명함**: 중앙은 거의 투명.
- **Rim**: 가장자리에는 얇은 밝은 rim.
- **Highlight**: 빛이 반사되는 작은 highlight.
- **Iridescence**: 아주 약한 pink / cyan / violet / yellow 계열의 비눗막 느낌.

과한 rainbow gradient는 피한다. "무지개 원"이 아니라 "투명한 막에서 색이 아주 살짝 보인다"가 목표다.

## 21. 막대 디자인

막대 역시 사실적인 제품 사진처럼 만들 필요 없다. 단순하고 귀여운 형태면 된다.

```
       ○
       │
       │
       │
       │
```

하단 손잡이 색은 나중에 정한다. 중요한 것은 사용자가 보자마자 "아 여기서 비눗방울 나오는구나."라고 이해하는 것이다.

## 22. UI

플레이가 시작된 후에는 UI를 거의 없앤다. 화면 대부분이 장난감이다. 가능하면 `후— 불어봐`조차 첫 성공 이후 사라진다. 설정, 메뉴, 점수판 등을 화면에 항상 표시하지 않는다.

## 23. 화면 구성

모바일 portrait 기준.

```
┌─────────────────────┐
│                     │
│   ◯           ○     │
│                     │
│         ◎           │
│                     │
│                     │
│                     │
│      후—            │
│                     │
│         ○           │
│         │           │
│         │           │
│         │           │
└─────────────────────┘
```

상단 약 70~80%는 방울들이 떠다니는 공간. 하단은 새 방울을 만드는 영역.

## 24. 세션

별도의 완료 상태가 없다. 사용자가 계속 만들 수 있다. 화면에 점점 방울이 많아지는 것 자체가 세션의 변화다.

## 25. v0.1 MVP

첫 번째 구현에서는 아래까지만 만든다.

1. 모바일 세로 fullscreen canvas
2. 비눗방울 막대
3. 마이크 권한
4. Web Audio API 입력
5. 바람 threshold 감지
6. 불면 막 끝에 방울 생성
7. 계속 불면 크기 증가
8. 방울 wobble
9. 불기를 멈추면 release
10. 위로 둥실거리며 이동
11. 화면 밖으로 나가면 제거
12. floating bubble 터치 시 pop
13. 바로 다음 방울 생성 가능

여기까지가 완전한 v0.1이다.

## 26. v0.1에서 만들지 않을 것

```
❌ 로그인 / 회원가입 / 서버 / DB
❌ 점수 / 랭킹 / 레벨 / 코인 / 미션
❌ 결과 화면 / 공유 / 컬렉션
❌ 캐릭터 / 상점 / 스킨 시스템
❌ 복잡한 particle physics
❌ 실제 fluid simulation
❌ 완벽한 breath AI classifier
```

기능을 추가하기 전에 "후—— → 뽀오오옹 → 똑 → 둥실"이 재미있는지부터 확인한다.

## 27. 구현 전략

첫 버전은 **Canvas 2D** 권장. Three.js를 먼저 사용하지 않는다. 비눗방울은 2D Canvas만으로도 충분히 아름답게 만들 수 있다.

장점: 모바일 성능 / 빠른 프로토타이핑 / touch hit testing 간단 / particle 구현 간단 / 여러 bubble 관리 쉬움.

필요해졌을 때 WebGL로 전환한다.

## 28. 구조

```
src/
├── audio/
│   └── breathDetector.ts
│
├── bubble/
│   ├── Bubble.ts
│   ├── BubbleManager.ts
│   └── bubbleRenderer.ts
│
├── particles/
│   └── PopParticles.ts
│
├── input/
│   └── touch.ts
│
├── config/
│   └── tuning.ts
│
├── canvas/
│   └── BubbleCanvas.tsx
│
├── App.tsx
└── main.tsx
```

과도한 architecture를 만들지 않는다.

## 29. 튜닝 값을 코드와 분리

이 프로젝트는 튜닝이 굉장히 중요하다. 따라서 아래 값들을 여러 파일에 hard-code하지 않는다.

```ts
export const tuning = {
  breathThreshold: ...,
  breathSmoothing: ...,

  bubbleMinRadius: ...,
  bubbleMaxRadius: ...,
  growthRate: ...,

  releaseDelay: ...,

  wobbleAmount: ...,
  wobbleSpeed: ...,

  floatSpeed: ...,
  horizontalDrift: ...,

  maxBubbleCount: ...
}
```

나중에 실제 휴대폰으로 테스트하면서 쉽게 변경할 수 있어야 한다.

## 30. 개발용 Debug Mode

실제 개발 중에는 debug mode를 제공해도 된다.

```
Mic      0.34
Breath   0.61
State    blowing
Radius   84
```

그리고 마이크 없이도 테스트할 수 있도록 Space / 화면 특정 영역 누르기 = 가짜 breath 같은 개발용 입력을 만들어도 좋다.

**중요**: 이 UI는 production에서는 전부 숨긴다.

## 31. 개발 순서

Claude는 전체를 한 번에 구현하지 말고 아래 순서로 진행한다.

### Phase 1 — Bubble Physics

마이크 없이 시작한다. 화면에 막대를 그리고 가짜 breath input을 만든다.

- 누르고 있으면: 방울 커짐
- 손을 떼면: release → floating
- 방울을 누르면: pop

여기까지 먼저 완성한다.

### Phase 2 — Feel

그 다음: wobble / release deformation / floating drift / pop animation을 튜닝한다. 이 단계에서 이미 장난감으로 재미있어야 한다.

### Phase 3 — Microphone

그 다음에 Web Audio API를 연결한다. fake breath → real microphone breath로 교체한다.

### Phase 4 — Visual Polish

마지막으로 transparency / highlight / iridescence / subtle shadow / particles를 다듬는다.

## 32. 가장 중요한 테스트

데스크톱에서만 테스트하지 않는다. 반드시 실제 스마트폰에서 휴대폰을 얼굴 앞에 놓고 `후——` 해본다.

특히 확인해야 할 것:

- **불기 시작하는 순간**: 방울이 바로 반응하는가?
- **계속 불 때**: 내 숨으로 키우고 있다는 느낌이 드는가?
- **불기를 멈췄을 때**: 방울이 자연스럽게 떨어지는가?

이 세 가지가 디자인보다 중요하다.

## 33. 성공 기준

사용자에게 설명하지 않고 폰을 건넨다. 화면에는 `후— 불어봐`만 있다. 사용자가 실제로 분다. 방울이 커진다. 사용자가 "오?" 한다. 한 번 더 분다. 더 큰 걸 만들어본다. 이번에는 방울을 터뜨려본다. 그리고 또 분다.

여기까지 자연스럽게 나오면 프로토타입은 성공이다.

---

## Claude에게 마지막으로 전달할 구현 지시

`bubble-breath`의 첫 번째 목표는 완성된 서비스를 만드는 것이 아니라 마이크 기반 interaction prototype을 만드는 것이다.

전체 기능을 한 번에 구현하지 말 것.

우선 마이크를 연결하지 않은 상태에서 모바일 fullscreen Canvas에 비눗방울 막대를 만들고, 개발용 fake breath input을 사용해 "누르고 있는 동안 방울 성장 → 놓으면 막에서 떨어짐 → 위로 둥실거림 → 터치하면 터짐"의 전체 interaction loop부터 구현한다.

이 loop가 제대로 작동한 다음 Web Audio API를 연결해 fake breath를 실제 마이크 breath input으로 교체한다.

비눗방울은 단순한 원의 scale animation처럼 보이면 안 된다. 성장 중 약한 wobble이 있어야 하며, release 순간에는 막에서 떨어지는 작은 deformation이 느껴져야 한다.

실제 fluid simulation이나 복잡한 물리엔진은 사용하지 않는다.

UI는 최소화하고 점수, 결과 화면, 로그인, DB, 공유 등의 기능은 구현하지 않는다.

가장 중요한 품질 기준은 사용자가 휴대폰에 `후——` 불었을 때 화면 속 방울과 물리적으로 연결되어 있다고 느끼는 것이다.
